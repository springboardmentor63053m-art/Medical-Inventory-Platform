spring.application.name=medistock-backend

# PostgreSQL Database Configuration
spring.datasource.url=jdbc:postgresql://localhost:5432/medistock_db
spring.datasource.username=postgres
spring.datasource.password=nanthidha@1452006
spring.datasource.driver-class-name=org.postgresql.Driver

# Hibernate / JPA Configuration
spring.jpa.hibernate.ddl-auto=create
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect

# JWT Configuration
# Note: In production, the secret should be securely injected via environment variables.
medistock.app.jwtSecret=MedistockSecretKeyForJwtAuthenticationWhichShouldBeVeryLongToBeSecureEnough1234567890
medistock.app.jwtExpirationMs=86400000

# Server Configuration
server.port=8081