spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/medistock_test
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver
  jpa:
    show-sql: false
    hibernate:
      ddl-auto: create-drop
